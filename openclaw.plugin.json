{
  "id": "claude-runner",
  "name": "Claude Code Runner",
  "description": "Uses Claude Agent SDK as a local provider — delegates all reasoning, tool use, and file editing to the SDK.",
  "providers": ["claude-runner"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "port": {
        "type": "number",
        "description": "Port for the local bridge server"
      },
      "workDir": {
        "type": "string",
        "description": "Working directory for the SDK (default: ~/.openclaw/workspace)"
      },
      "skipPermissions": {
        "type": "boolean",
        "description": "Use bypassPermissions mode"
      },
      "defaultModel": {
        "type": "string",
        "description": "Default Claude model to use"
      },
      "maxTurns": {
        "type": "number",
        "description": "Max agentic turns per request (safety cap, default: 30)"
      },
      "queueMinDelayMs": {
        "type": "number",
        "description": "Minimum delay between SDK queries in ms (default: 1000)"
      },
      "queueMaxDelayMs": {
        "type": "number",
        "description": "Maximum delay between SDK queries in ms (default: 4000)"
      },
      "queueMaxConcurrency": {
        "type": "number",
        "description": "Max concurrent SDK queries (default: 1)"
      },
      "sessionTtlMs": {
        "type": "number",
        "description": "Session cache TTL in ms (default: 3600000)"
      },
      "effort": {
        "type": "string",
        "description": "Effort level: low, medium, high, max (default: medium)"
      },
      "maxBudgetUsd": {
        "type": "number",
        "description": "Max dollar budget per request"
      },
      "tools": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Allowed tools list (default: all claude_code tools)"
      },
      "strictMcpConfig": {
        "type": "boolean",
        "description": "When true (the default), the SDK session uses only the MCP servers given in mcpServers and ignores MCP configuration it would otherwise discover on the filesystem. Set it to false to restore the SDK's own default."
      },
      "mcpServers": {
        "type": "object",
        "additionalProperties": { "type": "object" },
        "description": "MCP servers passed into the SDK query options as mcpServers. Keyed by server name; each value is a transport object the Agent SDK understands, e.g. { \"type\": \"http\", \"url\": \"http://127.0.0.1:3010/mcp\" }."
      }
    }
  }
}
